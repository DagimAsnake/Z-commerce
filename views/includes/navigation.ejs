<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-warning">
  <div class="container-fluid">
    <a class="navbar-brand fw-bolder fs-5" href="/">Zcommerce</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a
          class="<%= path === '/' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/"
          >Shop</a
        >
        <% if(isAuthenticated) {%>
        <a
          class="<%= path === '/cart' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/cart"
          >Cart</a
        >
        <a
          class="<%= path === '/orders' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/orders"
          >Order</a
        >

        <a
          class="<%= path === '/admin/addProduct' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/admin/addProduct"
          >Add Product</a
        >
        <a
          class="<%= path === '/admin/products' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/admin/products"
          >Admin Product</a
        >
        <% } %>
      </div>
      <div class="navbar-nav ms-auto">
        <% if(!isAuthenticated) {%>
        <a
          class="<%= path === '/login' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/login"
        >
          Login
        </a>
        <a
          class="<%= path === '/signup' ? 'active nav-link fw-bolder fs-5' : 'nav-link fw-bolder fs-5' %>"
          href="/signup"
        >
          Signup
        </a>
        <% } else {%>
        <form class="nav-link bg-warning" action="/logout" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <button
            class="bg-danger border rounded-5 p-1 text-white border-danger"
          >
            Logout
          </button>
        </form>
        <% } %>
      </div>
    </div>
  </div>
</nav>
